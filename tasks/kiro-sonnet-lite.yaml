# Kiro CLI + Sonnet 4.6, TB2 lite (51 tasks)

dataset: terminal-bench/terminal-bench-2
task_list: tb2-lite.txt

agent:
  name: kiro_cli
  import_path: agents.kiro_cli.harbor_agent:KiroCliAgent
  model: claude-sonnet-4.6

run:
  concurrent: 4

judge:
  model: claude-opus-4.6
  rubric: default
  failed_only: false
