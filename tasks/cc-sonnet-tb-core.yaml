# Claude Code + Sonnet 4.6 via Bedrock, full 80 tasks

dataset: terminal-bench-core

agent:
  name: claude_code
  model: global.anthropic.claude-sonnet-4-6
  kwargs:
    version: "2.1.101"

run:
  concurrent: 4

judge:
  model: claude-opus-4.6
  rubric: default
  failed_only: false
