# tasks/kiro-qwen-tb-core.yaml

dataset: terminal-bench-core

agent:
  name: kiro_cli
  model: qwen3-coder-next

run:
  concurrent: 4

judge:
  model: claude-opus-4.6
  rubric: default
  failed_only: true
