dataset: terminal-bench-core

agent:
  name: claude_code
  model: global.anthropic.claude-sonnet-4-6
  kwargs:
    version: "2.1.101"

run:
  concurrent: 2
  tasks:
    - hello-world
    - fix-permissions
    - csv-to-parquet

judge:
  model: claude-opus-4.6
  rubric: default
  failed_only: false
