# tasks/kiro-sonnet-tb-core-noprompt.yaml
# Baseline: no system prompt, for comparison

dataset: terminal-bench-core

agent:
  name: kiro_cli
  model: claude-sonnet-4.6

run:
  concurrent: 4

judge:
  model: claude-opus-4.6
  rubric: default
  failed_only: false
