# tasks/kiro-minimax-tb-core.yaml

dataset: terminal-bench-core

agent:
  name: kiro_cli
  model: minimax-m2.5

run:
  concurrent: 4

judge:
  model: claude-opus-4.6
  rubric: default
  failed_only: true
